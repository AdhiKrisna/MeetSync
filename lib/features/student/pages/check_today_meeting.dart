import 'package:flutter/material.dart';

class CheckTodayMeeting extends StatelessWidget {
  CheckTodayMeeting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Check Today Meeting'),
      ),
    );
  }
}